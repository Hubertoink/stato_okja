# These are all values requested by the ZimaOS import form.
# Generate both secrets once, keep them safe and do not change them on updates.

STATO_VERSION=1.6.0
WEBUI_PORT=8088
STATO_URL=http://192.168.1.50:8088
STATO_HTTPS=false

POSTGRES_PASSWORD=CHANGE_TO_A_LONG_UNIQUE_PASSWORD
JWT_SECRET=CHANGE_TO_A_LONG_RANDOM_SECRET_WITH_AT_LEAST_48_CHARACTERS
SUPERADMIN_EMAIL=admin@example.org
