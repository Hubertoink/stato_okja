## Nginx config is now selected at build time.
## See nginx.direct.conf (no proxy) and nginx.proxy.conf (Docker proxy to backend).
## The Dockerfile copies one of them to /etc/nginx/conf.d/default.conf based on NGINX_MODE.
